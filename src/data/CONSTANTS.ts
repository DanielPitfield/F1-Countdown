// Jolpica-f1
export const BASE_API_URL = "https://api.jolpi.ca/ergast/f1";

// The maximum number of data entries that can be returned by an API response (if no limit is specified, the default limit is 30)
export const MAX_LIMIT = 100;
